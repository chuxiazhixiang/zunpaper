__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.env.local

# Local-only artifacts
tmp/
scripts/.cache/
*.pdf

# Local workspace assets — the deployed copy lives in site/assets/img/
/setsuna.jpg

# Live2D source drop — the deployed copy lives in site/assets/live2d/LSS/
/LSS/
