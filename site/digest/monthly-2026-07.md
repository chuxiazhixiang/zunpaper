# 2026-07 月度领域综述

_由 Zunpaper（deepseek-v4-flash）自动生成，覆盖 387 篇论文 / 视频；2026-07-31_

**一句话：** 世界模型与VLA深度融合，数据效率与实时性成焦点

## 一句话
世界模型与VLA深度融合，数据效率与实时性成焦点。

## 五大主题
### 世界模型从视频生成走向物理与动作控制
本月世界模型不再只是视频预测，而是与动作、物理深度绑定。`PhiZero` 用物理语言显式推理世界演化，再渲染成视频，保证物理一致性；`ShadowDancer` 通过影子对学习统一动力学表示，实现任意动作控制。`QQWorld` 用分位数匹配正则化世界模型，改善尾部控制；`Cycle-World` 通过反向预测循环一致性缓解长视频误差累积。这些工作让世界模型从“看”进化到“做”，为机器人规划提供更可靠的想象空间。

### VLA 模型走向轻量化与实时推理
VLA 模型在追求性能的同时，开始强调部署效率。`TurboVLA` 在 RTX 4090 上实现 32 Hz 实时推理，显存低于 1 GB；`CoTinyVLA` 通过思维链蒸馏，0.9B 参数超越 7B 模型。`Reflex` 通过流式推理加速流匹配 VLA 模型 2.58 倍；`ActionCache` 缓存中间动作实现 34 倍加速。这些工作让 VLA 模型真正具备实时控制机器人的潜力，而非停留在离线演示。

### 触觉与视觉融合成为操作任务的关键
接触丰富的操作任务中，触觉信息不可或缺。`FasTac` 曲面视觉触觉传感器实现高速高精度三维形状与力感知；`ViTacWorld` 用世界模型生成视觉-触觉轨迹，提升密集接触操作性能。`VT-WAM` 联合预测视觉和触觉形变，显著提升操作成功率。`FeelWorld` 分层视触觉世界模型联合预测视觉与触觉状态，提升接触任务规划。触觉不再是辅助，而是与视觉平起平坐的核心模态。

### 数据获取与利用效率大幅提升
数据仍是机器人学习的瓶颈，但本月多项工作试图打破这一限制。`ACE-Data-0` 将家庭环境变为数据引擎，生成多模态同步数据；`DexDirect` 通过直接动觉引导高效收集灵巧演示。`HiFi-UMI` 仅用高保真无机器人数据训练策略，可直接部署于真实机器人。`PRISM` 从单张图像生成个性化数据集，无需遥操作。`SIEVE` 用 50% 数据超越全量训练。数据获取从“堆量”转向“提质”。

### 测试时适应与鲁棒性成为研究热点
面对分布外场景，测试时适应成为提升鲁棒性的重要手段。`RedFlow` 将失败重定向为动作级修正，提升 VLA 策略成功率；`PriGo` 通过测试时原始引导提升扩散和流策略的泛化。`WAM-TTT` 在测试时从人类视频学习自适应记忆，引导世界-动作模型。`IDR` 框架在测试时动态调整视觉重要性。这些工作让模型在部署后仍能持续改进，而非一成不变。

## 值得抄作业的工作
- `TurboVLA`：值得抄“直接映射视觉语言到动作”的极简设计，在 4090 上实现 32 Hz 实时推理，部署门槛极低。
- `HiFi-UMI`：值得抄“仅用高保真无机器人数据训练策略”的流程，大幅降低数据采集成本。
- `ActionCache`：值得抄“缓存中间动作”的推理加速思路，无需训练即可获得 34 倍加速。
- `SIEVE`：值得抄“结构感知数据选择”方法，用 50% 数据超越全量训练，数据效率极高。
- `Cycle-World`：值得抄“反向预测循环一致性”的误差累积缓解方案，对长时域世界模型有普适价值。

## 还没解决的事
- 世界模型的长时域生成仍存在误差累积，`Cycle-World` 虽缓解但未根治，运动学与动力学失败原因尚未完全分离。
- VLA 模型的测试时适应仍依赖人工干预或额外数据，`WAM-TTT` 等方法的自动化程度有限，且未解决灾难性遗忘。
- 触觉传感器的硬件成本与集成难度仍高，`FasTac` 等虽性能优异，但尚未普及到低成本机器人平台。
- 数据获取的自动化程度不足，`ACE-Data-0` 等仍需人工布置环境，无法完全自主采集。
- 多模态融合中模态缺失的鲁棒性仍待加强，`MAMMOTH` 虽提出模态丢弃训练，但真实场景中模态缺失模式更复杂。